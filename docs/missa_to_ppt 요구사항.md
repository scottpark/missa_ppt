# 미사 PPT 작성 요구사항

- 최종 수정일: 2026-08-25
- 이 문서는 버전 번호 없이 항상 현재 시스템의 요구사항을 처음부터 끝까지 담은 최신 완결
  문서로 유지한다(수정할 때마다 새 버전 파일을 만들지 않고 이 파일 자체를 갱신). 언제 무엇이
  바뀌었는지는 맨 끝 "부록: 변경 이력"에 날짜순으로 추가한다. v1.0~v1.4의 과거 버전 원문은
  `docs/archive/`에 그대로 보존되어 있으나, 그 이후로는 별도 버전 번호를 매기지 않는다.

## 개요

한국천주교주교회의 매일미사 사이트(missa.cbck.or.kr)에서 날짜별 미사 내용을 가져와서, 이전 미사의 PPT 파일을 수정하여 새 미사 날짜의 PPT 파일을 만든다. 주일미사와 평일미사 모두 지원하며, 입력 날짜의 요일로 미사 유형을 자동 판단한다. 참조 PPT의 폰트·서식·레이아웃·배경은 그대로 유지하고 텍스트와 슬라이드 구성만 교체하는 것이 이 프로그램의 핵심 원칙이다.

## 미사 유형 자동 판단

- 입력 날짜가 일요일이면 → 주일미사 처리
- 입력 날짜가 일요일이 아니면(토요일 포함) → 평일미사 처리
- 사용자가 별도로 미사 유형을 선택하지 않음
- 처리 시작 시 "미사 유형: 주일미사 / 평일미사" 로그 출력
- (참고) 토요일도 현재는 평일미사로 처리됨. 토요일 전야미사 등 별도 로직이 필요해지면
  `is_sunday_mass()` 판단 로직 자체를 확장해야 하며, `test_missa_regression.py`의
  `TestIsSundayMass.test_saturday_currently_treated_as_weekday`가 이 변경의 회귀 기준점이 된다.

## 실행 방식

### (A) 대화형 모드 — 인수 없이 실행

```
python missa_to_ppt.py
```

팝업 순서:

1. 미사 일자 입력 (YYYYMMDD 텍스트 입력)
2. 파일 선택 화면 [첫 번째 팝업]
   - 참조 미사 PPT: 필수
   - 화답송 악보 PPT: 선택 (주일미사에만 필요; 미입력 시 경고 후 진행 확인)
   - 시작기도 PPT: 선택
   - 미사 후 기도 PPT: 선택 (평일미사에만 사용)
3. 성가번호 입력 [두 번째 팝업] (입당·봉헌·성체·2차봉헌·파견) — 주일미사는 5종 전부, 평일미사는
   2차봉헌을 제외한 4종이 필수 입력이며, 숫자 형식도 함께 검증한다. 누락/형식 오류 시 팝업으로
   안내하고 다시 입력받는다

### (B) CLI 모드 — 인수 지정 실행

```
python missa_to_ppt.py YYYYMMDD [--입당 N --봉헌 N --성체 N --2차봉헌 N --파견 N] [--화답송 PATH] [--미사후기도 PATH] [--test]
```

- 성가번호 생략 시 팝업으로 입력받음
- `--test`: 팝업 없이 폴더 파일로 번호를 자동 추론 (자동화 테스트용)
- 입력 파일은 날짜 폴더(`YYYYMMDD/`)에서 자동 탐색하며, `--화답송`/`--미사후기도`로 경로를 직접 지정할 수 있음

### (C) 성가 폴더 최초 설정

두 실행 방식 모두 성가 PPT는 OneDrive 동기화 폴더에서 찾는다. 폴더 경로가 아직 `config.json`에
없으면 최초 1회 폴더 선택 팝업이 뜨고, 이후에는 자동으로 로드된다(팝업 없음).

### (D) 오류 알림 방식

실행 중 오류가 발생하면(성가 번호/파일 누락 등) 팝업에는 **해당 오류 메시지만** 간결하게
표시한다. 처리 과정의 전체 로그(정상 진행 내용 포함)는 팝업에 늘어놓지 않고 항상
`YYYYMMDD/log/` 폴더의 로그 파일로 저장한다 — 오류 여부와 무관하게 매 실행마다 로그 파일이
남는다. 이는 주일미사·평일미사 구분 없이 동일하게 적용된다.

## 입력 파일

1. **참조 미사 PPT**
   - 이전 미사 PPT (이번 미사의 기준 양식). 날짜 폴더 내에서 파일명에 오늘 날짜(`YYYYMMDD`)가
     포함되지 않은 `.pptx` 중 선택됨(성가/시작기도/화답송악보 파일 제외)
   - 대화형 모드: 직접 파일 선택 (필수)
   - CLI 모드: `YYYYMMDD/` 폴더에서 자동 탐색

2. **화답송 악보 PPT**
   - 대화형 모드: 직접 파일 선택 (선택; 주일미사에서 미입력 시 경고 후 진행 확인)
   - CLI 모드: `YYYYMMDD/` 폴더에서 자동 탐색 (파일명에 "화답송 악보" 포함)
   - 평일미사: 사용하지 않음

3. **시작기도 PPT**
   - 대화형 모드: 직접 파일 선택 (선택)
   - CLI 모드: `YYYYMMDD/` 폴더에서 자동 탐색 (파일명에 "시작기도" 포함)

4. **미사 후 기도 PPT**
   - 평일미사 전용; 주일미사에는 사용하지 않음
   - 대화형 모드: 직접 파일 선택 (선택)
   - CLI 모드: `YYYYMMDD/` 폴더에서 자동 탐색 (파일명에 "미사후기도" 포함)

5. **성가 PPT** (입당·봉헌·성체·2차봉헌·파견)
   - 주일미사: OneDrive 동기화 폴더에서 우선 자동 탐색(파일명에 "성가 NNN" 포함), 못 찾으면
     날짜 폴더 fallback 탐색
   - 평일미사: 악보 슬라이드 자체를 사용하지 않으므로 OneDrive 폴더는 조회하지 않고, 번호
     슬라이드(divider) 갱신에만 성가번호를 사용
   - 폴더 경로는 최초 1회 팝업으로 입력받아 `config.json`에 저장, 이후 자동 로드
   - 평일미사: 번호 슬라이드만 갱신하며 악보 슬라이드는 복사하지 않음
   - **필수 악보 파일 검증**: 주일미사는 5종(입당·봉헌·성체·2차봉헌·파견) 전부, 평일미사는
     2차봉헌을 제외한 4종의 악보 파일을 (OneDrive/날짜 폴더 어디에서도) 찾지 못하면, 어떤
     성가 악보가 없는지 명시한 오류 메시지를 내고 처리를 중단한다(성가 **번호** 누락 검증과는
     별개로, 번호는 있으나 그 번호에 해당하는 실제 악보 **파일**이 없는 경우를 잡는다).

6. **미사 JSON**
   - `missa_to_json.py`를 실행해 missa.cbck.or.kr을 크롤링, 자동 생성 (`YYYYMMDD/missa_YYYYMMDD.json`)
   - 이미 존재하면 재크롤링 생략
   - 섹션: 입당송, 제1독서, 화답송, 제2독서(있는 경우), 복음환호송, 복음, 영성체송
   - 독서·복음 섹션은 `title`(성서명)과 `chapter_verse`(장·절 표기)를 분리해 저장
   - 독서·복음 `content`는 절 사이 개행을 제거해 하나의 문자열로 병합 (절 번호는 본문 안에 유지)
   - 화답송·입당송·영성체송 `content`는 줄 구분(`\n`)을 유지

## 처리 및 출력

### 공통 처리 (주일·평일 동일)

1. `missa_to_json.py` 실행 → JSON 생성 (이미 존재하면 재실행 생략)
2. 참조 PPT를 열어 JSON 내용으로 전례 텍스트 교체 (제목 슬라이드, 입당송, 제1독서, 제2독서, 복음환호송, 복음, 영성체송)
3. 시작기도문 슬라이드 교체 (파일 지정 시)
4. 성가 슬라이드 처리 (5종)
5. 검증
6. 출력 파일 저장: `YYYYMMDD/{liturgy}.pptx`

### 주일미사 추가 처리

- 화답송: 화답송 악보 PPT에서 악보 슬라이드 복사 + 텍스트 슬라이드 배치

### 평일미사 추가 처리

- 화답송: 텍스트 슬라이드만 배치 (악보 없음)
- 미사 후 기도: 파견성가 다음에 별도 PPT 슬라이드 삽입 (파일 지정 시)

## 세부 요구사항

### 1. 전례 텍스트 교체

**1-1. 제목 슬라이드**
- 날짜(연·월·일)와 전례명(liturgy) 교체, 기존 포맷 유지

**1-2. 입당송**
- content 앞에 "◎ \t" 추가, 기존 pPr(들여쓰기·탭 정렬) 유지

**1-3. 독서·복음 공통**
- 본문 내 숫자(절 번호)는 오렌지색 (RGB: 255, 192, 0)
- 같은 절 내 `\n`은 무시하고 이어서 표시
- 슬라이드당 최대 9줄; 초과 시 다음 슬라이드에서 이어서 표시. 마지막 슬라이드를 제외한 모든
  본문 슬라이드는 정확히 9줄을 채운다
- 줄 수는 단어 경계 word-wrap 기준으로 계산하되, 실제 PowerPoint 렌더링과 일치해야 한다.
  Pillow로 폰트 폭을 계산할 때는 실제 사용 폰트(예: Batang ≠ BatangChe — 같은 TTC 파일 안에
  서로 다른 서체가 들어 있으므로 인덱스를 실제로 확인해야 함)를 정확히 로드하고, Pillow가
  커닝(글자 조합별 자간 조정)을 지원하지 않는 데서 오는 폭 과대평가를 보정 계수로 반영한다
  (`missa_to_ppt.py`의 `_RENDER_WIDTH_CALIBRATION`, 현재 1.03 — 실측 검증된 값이며 폰트/크기
  조합이 크게 바뀌면 재검증 필요)
- **Pillow 추정만으로는 근본적으로 오차가 남을 수 있다** — 그 추정치가 정확히 슬라이드당 허용
  줄 수(9줄)에 걸쳐 있는 경우에 한해, 실제 PowerPoint를 통해 렌더링 줄 수를 실측 검증한다
  (§1-11 참고). 이 검증은 슬라이드 전체 줄 수뿐 아니라, 단락을 어디서 나눌지(분리 지점)까지
  재확인·재조정한다.
- "주님의 말씀입니다." 등 종료 텍스트는 본문과 별도 처리
- 절 하나 = run 2개(절 번호+본문)라고 가정하지 않는다. 여러 절이 continuation으로 하나의
  논리 단락에 병합된 경우 절 번호 run이 단락 중간에도 나타날 수 있으므로, 단락을 두 조각으로
  나누는 로직은 분리 지점이 몇 번째 run의 몇 번째 글자인지 실제로 찾아 그 앞뒤 run들의 서식
  (rPr, 오렌지색 포함)을 그대로 보존해야 한다.

**1-4. 독서·복음 종료 슬라이드 레이아웃**
- 마지막 슬라이드의 본문이 5줄 이하이면 종료 텍스트를 해당 슬라이드에 통합
- "주님의 말씀입니다." 텍스트박스는 본문 마지막 줄로부터 두 줄 공백 후 배치
- 제1독서·복음의 종료 슬라이드 텍스트박스 위치·크기는 제2독서의 종료 슬라이드를 기준으로 정렬
- 제1독서·제2독서·복음 각각 독립적으로 위치 계산
- 종료 텍스트가 통합된 슬라이드를 포함해, 모든 독서·복음 슬라이드의 배경(검정 도형)은 항상
  슬라이드 전체 크기를 덮어야 한다 (부분 도형과 혼동해 잘못된 크기로 축소되면 안 됨)

**1-5. 제1독서**
- 제목 슬라이드: 성서명만 교체 (절 번호 제거)
- 본문 슬라이드: 내용 교체

**1-6. 화답송**

- 주일미사
  - 악보 슬라이드(짝수 인덱스, 0부터 시작)와 텍스트 슬라이드(홀수 인덱스) 교대 배치: [악보]-[절1]-[악보]-[절2]-...
  - 악보 슬라이드: 화답송 악보 PPT의 슬라이드를 원본 서식(배경 포함) 그대로 복사 — 복사 후
    배경을 임의로 다시 칠하지 않는다. 화답송 악보 PPT와 참조 PPT의 슬라이드 크기가 다르면
    도형 위치·크기를 비율만큼 보정해 확대/축소되어 보이지 않게 한다(§1-12 참고).
  - 텍스트 슬라이드: 화답송 각 절 내용(`○ \t` + 내용). 후렴(◎)은 악보 슬라이드에 포함되므로 제외

- 평일미사
  - 텍스트 슬라이드만 배치 (악보 슬라이드 없음)
  - 참조 PPT의 화답송 텍스트 템플릿 슬라이드 포맷 사용 (템플릿 슬라이드는 `find_sections()`으로
    동적 탐지 — 슬라이드 번호 고정 불가)
  - 후렴(◎)도 텍스트로 표시 (주일과 달리 악보가 없어 후렴을 표시할 곳이 텍스트 슬라이드뿐)
  - 화답송 제목 갱신은 주일·평일 공통

**1-7. 제2독서**
- 제목 슬라이드: 성서명만 교체
- 본문 슬라이드: 내용 교체
- JSON이 null이면 제2독서 슬라이드 전체 삭제 (직전 blank divider도 함께 삭제해 연속 blank 방지)
- 평일미사에서는 제2독서가 없는 경우가 일반적이나, 대축일 등은 평일에도 있을 수 있음

**1-8. 복음환호송**
- 본문 교체. run-level 폰트 구조 보존(◎ 알렐루야 줄과 ○ 구절 줄의 서식이 다름)

**1-9. 복음**
- 제목 슬라이드: 복음사가 이름+조사 교체
- 본문 슬라이드: 내용 교체 (독서와 동일한 9줄 규칙 적용)

**1-10. 영성체송**
- 본문 교체

**1-11. 줄 수 실측 검증 (PowerPoint COM)**
- Pillow 추정치가 슬라이드당 허용 줄 수(9줄)와 정확히 같을 때만, 실제 PowerPoint를 통해
  그 슬라이드의 렌더링 줄 수를 재확인한다. 불일치하면 실측값을 채택하고 슬라이드 내용을
  재조정한다.
- 재조정 시 분리 지점(어느 지점에서 다음 슬라이드로 넘길지)도 Pillow 추정만으로 확정하지
  않고, 조정 결과를 다시 실측해 필요하면 분리 지점을 앞뒤로 옮겨 재시도한다. 여러 번
  시도해도 9줄에 정확히 맞지 않으면(오버플로가 남으면), 그 시도 전체를 되돌려 절대
  오버플로 상태를 "성공"으로 보고하지 않는다 — 다소 채워지지 않은(9줄 미만) 슬라이드로
  남는 것은 허용하되, 9줄을 넘는 슬라이드가 남는 것은 허용하지 않는다.
- 이 실측 검증은 `config.json`의 `com_verification_enabled`(기본 `true`) 플래그로 끌 수
  있다. 또한 PowerPoint COM을 사용할 수 없는 환경(pywin32 미설치, PowerPoint 미설치, 자동화
  실패)에서는 자동으로, 오류 없이 기존 Pillow 전용 방식으로 폴백한다 — 어느 경우든 프로그램
  실행 자체가 중단되지는 않는다.
- 다른 PowerPoint 버전(2007~365)이 설치된 여러 PC에서 실행될 수 있으므로, 실측은 그 PC에
  설치된 PowerPoint를 그대로 이용하며 특정 버전에 종속되지 않는다.

**1-12. 외부 프레젠테이션 슬라이드 복사 시 크기 보정**
- 화답송 악보, 성가, 시작기도, 미사 후 기도 등 다른 PPT 파일에서 슬라이드를 그대로 복사해
  올 때, 원본과 참조 PPT의 슬라이드 크기(가로세로 비율/치수)가 다르면 도형이 절대 좌표
  그대로 복사되어 확대되거나 잘려 보일 수 있다. 슬라이드 크기 비율만큼 도형의 위치·크기를
  자동으로 보정해, 원본과 동일한 비례로 보이도록 한다.
- 도형 위치·크기뿐 아니라 텍스트도 잘려 보이지 않도록 폰트 크기(`sz`)와 단락 여백/들여쓰기
  (`pPr`의 `marL`/`marR`/`indent`/`defTabSz`)도 같은 비율(가로·세로 축소 비율 중 더 작은 쪽)로
  함께 축소한다(예: "화답송 시편 138(137)"이 잘려 "화답송 시편"으로만 보이던 버그 수정).

### 2. 시작기도문 교체

- 입당송 슬라이드 직전에 위치
- 시작기도 앞의 빈 슬라이드 한 장은 보존 (삭제하지 않음)
- 선택한 시작기도 PPT 슬라이드로 전체 교체 (원본 서식·이미지·배경 그대로 복사)
- 파일 미지정 시 생략 (오류 아님)

### 3. 성가 처리 (순서: 입당→봉헌→성체→2차봉헌→파견)

- 주일미사
  - 성가 PPT 슬라이드를 원본 양식 그대로 복사 (악보 포함)
  - 슬라이드 상단 성가 타입 및 번호 업데이트
  - 원본 악보 첫 줄의 구분 라벨(예: "2차 봉헌")이 이번 주 실제 용도(예: 입당 성가로 쓰이는
    경우)와 다를 수 있으므로, 번호뿐 아니라 라벨도 이번 주 용도에 맞는 표준 표기로 함께
    교정한다. 라벨을 찾지 못하면 경고 로그만 남기고 처리는 계속한다

- 평일미사
  - 성가 번호 슬라이드(divider)만 갱신 (성가 타입 및 번호 업데이트)
  - 악보 슬라이드는 복사하지 않으며, 참조 PPT에 남아 있는 기존 악보 슬라이드는 삭제

### 4. 미사 후 기도 (평일미사 전용)

- 파견성가 다음(파견 콘텐츠 → blank divider → 미사 후 기도 → blank → 파견 순서 참조)에
  별도 PPT 슬라이드들을 순서대로 삽입
- 원본 서식(이미지·배경 포함) 그대로 복사
- 파일 미지정 시 생략 (오류 아님, 기존 미사 후 기도 슬라이드가 있으면 그대로 보존)

### 5. 검증

**5-1. 실행 시 자동 검증 (validate)**
- liturgy 텍스트가 PPT 안에 존재하는지 확인 (오류)
- 입당송·영성체송 슬라이드 존재 확인 (경고). 주일미사는 화답송 슬라이드 존재도 함께 확인
- 제1독서가 있으면 독서 절 번호가 실제로 오렌지색(RGB 255,192,0) run으로 렌더링됐는지
  절 번호 단위로 대조(텍스트만 남고 색상만 사라지는 경우도 잡아낸다) (경고)
- 오류가 하나라도 있으면 검증 실패로 처리, 경고는 결과를 막지 않고 로그에만 표시

**5-2. PPTX 구조 검증**
- 저장 직후 PPTX(OOXML) 구조 자체가 손상 없이 열리는지 별도 확인

**5-3. 회귀 테스트 (`test_missa_regression.py`, `test_ppt_com_verify.py`)**
- 코드 변경(신규 기능 추가·리팩터링) 후 `pytest test_missa_regression.py test_ppt_com_verify.py -v`로 실행
- 단위 테스트: `is_sunday_mass()` 요일 판단, 줄바꿈 계산, 절 번호 파싱, 단락 분리 시 서식
  보존, COM 조정 로직(경계값 최적화·재시도·give-up 롤백), config 플래그, PowerPoint COM
  래퍼(`ppt_com_verify.py`) 자체
- 통합 테스트: 주일(20260712)·평일(20260624) 날짜로 실제 PPT를 생성해
  - 출력 파일이 손상 없이 열리는지
  - 미사 유형이 로그에 올바르게 찍히는지
  - 필수 섹션이 모두 존재하는지
  - 독서·복음 본문 슬라이드가 9줄을 넘지 않는지(Pillow 기준 + 실제 PowerPoint COM 실측 기준
    양쪽 모두)
  - 절 번호가 슬라이드/단락 순서대로 오름차순으로 나타나는지(단락 순서 뒤바뀜 회귀 방지)
  - 배경(검정 도형)이 항상 슬라이드 전체를 덮는지
  - 화답송·성가가 주일/평일 패턴에 맞게 처리됐는지
  를 검증한다
- pywin32/PowerPoint COM을 쓸 수 없는 환경에서는 COM 전용 테스트가 자동으로 skip된다(기존
  Pillow 기반 테스트는 환경과 무관하게 항상 실행됨)
- 새 미사 유형(예: 토요일미사)이나 새 기능을 추가할 때는 `MASS_CASES`에 케이스를 추가하고
  회귀 여부를 확인한 뒤 병합한다

## 부록: 변경 이력

v1.4까지는 버전 번호로 관리했다(원문은 `docs/archive/`에 보관). 그 이후로는 버전 번호를 매기지
않고 이 문서 자체를 계속 갱신하며, 무엇이 바뀌었는지만 날짜순으로 아래에 추가한다.

| 시점 | 주요 변경 |
|---|---|
| v1.0 | 최초 구현. 주일미사 전용, CLI 인수 실행만 지원 |
| v1.1 | 대화형 팝업 모드 추가, 성가 PPT OneDrive 중앙 관리, 독서·복음 종료 슬라이드 텍스트박스 위치 요구사항 추가 |
| v1.2 | 평일미사 지원 추가(날짜 기반 자동 판단), 화답송·성가·미사 후 기도 처리를 주일/평일로 분기 |
| v1.3 | v1.2에서 정의된 요구사항(9줄 제한, 화답송 서식 보존, 배경 전체 커버)이 구현상 지켜지지 않던 버그 수정, 회귀 테스트 스위트(`test_missa_regression.py`) 추가. 이 버전부터 요구사항 문서를 "변경 이력" 대신 "현재 전체 요구사항"을 담은 완결 문서로 관리 |
| v1.4 (2026-08-04) | 절 하나=run 2개 가정을 없애 단락 분리 시 절 번호 서식 유실 버그 수정(§1-3) 및 검증 강화(§5-1). 성가 악보 파일 필수 검증(주일 5종/평일 4종) 및 오류 팝업 간소화(§(D), 입력 파일 5) 추가. 외부 프레젠테이션(화답송 악보 등) 복사 시 슬라이드 크기 차이로 인한 확대 버그 수정(§1-12). Pillow 추정만으로 남는 오차를 실제 PowerPoint COM 실측으로 검증·재조정하는 하이브리드 방식 도입 — 분리 지점 재조정 포함, config 플래그로 on/off, COM 불가 시 자동 폴백(§1-11) |
| 2026-08-25 | (문서 버전 번호 매기기 중단, 이 문서를 항상 최신 상태로 유지하는 방식으로 전환) 성가 번호 입력 팝업에 필수/형식 검증 추가(실행 방식 3). 평일미사는 악보가 불필요하므로 OneDrive 폴더 자체를 조회하지 않도록 변경(입력 파일 5). 외부 프레젠테이션 복사 시 폰트 크기·단락 여백도 함께 축소해 텍스트 잘림 방지(§1-12). 성가 헤더 구분 라벨을 이번 주 실제 용도에 맞게 교정하는 로직 추가(§3) |

과거 버전(v1.0~v1.3)의 원문은 `docs/archive/`에 보관되어 있다.
